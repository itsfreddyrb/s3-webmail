import React from 'react';

const ReplyToButton = (props) => {
    const replyTo = (event) => {
        
    };
    return (
        <i className="fa fa-reply"
            aria-hidden="true"
            onClick={replyTo}
            ></i>
    );
};

export default ReplyToButton;
